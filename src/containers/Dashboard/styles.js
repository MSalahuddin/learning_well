// @flow
import { StyleSheet } from "react-native";
import { Colors, Metrics } from "../../theme";

export default StyleSheet.create({
  mainContainer: { flex: 1, backgroundColor: "#eaedf2", opacity: 0.97 },
  container: {
    height: Metrics.screenHeight
    // backgroundColor: "red"
    // borderWidth: 2,
    // borderColor: "black"
  }
});
